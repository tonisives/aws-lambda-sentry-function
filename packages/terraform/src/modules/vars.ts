import { Construct } from "constructs";
import * as aws from '@cdktf/provider-aws';

// add env vars directly to main.ts, into environment object for each lambda
// This should be used if want to add variables to ssm
export const addVars = (app: Construct) => {

}
